{
    "name": "life-goods",
    "private": true,
    "scripts": {
        "dev": "pnpm --dir frontend dev",
        "dev:http": "pnpm --dir frontend dev:http",
        "dev:https": "pnpm --dir frontend dev:https",
        "build": "pnpm --dir frontend build",
        "test": "pnpm --dir frontend test && pnpm backend:test",
        "typecheck": "pnpm --dir frontend typecheck && pnpm backend:typecheck",
        "backend:typecheck": "pyright -p backend",
        "lint": "pnpm frontend:lint:check && pnpm backend:lint",
        "frontend:lint:check": "prettier --check frontend && pnpm --dir frontend lint",
        "frontend:lint:format": "prettier --write frontend",
        "backend:install": "uv sync --project backend --dev",
        "backend:sync": "uv sync --project backend --dev",
        "backend:dev": "uv run --project backend uvicorn lifegoods.main:app --app-dir backend/src --reload",
        "backend:test": "uv run --project backend pytest backend/tests",
        "backend:test:integration": "uv run --project backend pytest backend/tests --run-integration",
        "backend:lint": "uv run --project backend ruff check backend/src backend/tests backend/scripts",
        "off:dataset": "uv run --project backend python -m lifegoods.open_food_facts.cli --mongo-uri mongodb://lifegoods_writer:lifegoods_writer@localhost:27018/lifegoods_off",
        "generated-data:init": "uv run --project backend python -m lifegoods.generated_data.cli init",
        "generated-data:verify": "uv run --project backend python -m lifegoods.generated_data.cli verify",
        "benchmark:run": "uv run --project backend python -m lifegoods.translation.benchmark.cli run --mode offline",
        "benchmark:live": "uv run --project backend python -m lifegoods.translation.benchmark.cli run --mode live",
        "api:generate": "uv run --project backend python backend/scripts/export_openapi.py && pnpm --dir frontend api:generate",
        "api:check": "pnpm api:generate && git diff --exit-code -- frontend/openapi.json frontend/src/api/generated"
    },
    "devDependencies": {
        "prettier": "^3.9.6",
        "prettier-plugin-tailwindcss": "^0.8.1",
        "pyright": "^1.1.400"
    }
}
